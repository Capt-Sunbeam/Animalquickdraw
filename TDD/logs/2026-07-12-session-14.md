# Session 14 — 2026-07-12

**Focus:** Titles/achievements design refinement (owner) → Slice 19 (mini): emoji retirement + title/ceremony rework → Slice 14: Achievements & Stats (Chunk 17) → owner live-test → Slice 20 (mini): authentic undo replays + D-key fixes → owner-confirmed, D-drag extension, owner-confirmed again
**Result:** THREE slices IMPLEMENTED in one session — Slice 20 fully OWNER-CONFIRMED live; 19/14 machine-verified with feel-checks batched. 556 → 581 tests green; all 3 gates PASS (run twice). **Owner started the Steamworks registration** (the Slice 15 critical path).

---

## Objectives & outcomes

1. **Session-start + state discussion.** Roadmap-to-release laid out; the owner's judging-grid concern (5+ players) analyzed with real layout math — confirmed justified (min cell sizes force scrolling at 7 entries; prominent bottom chat eats ~22% height; portrait drawings render ~163 px wide in cells).
2. **Owner design decisions (all logged 2026-07-12):**
   - **Emoji reaction system removed entirely** — weakest reward surface + judging-grid clutter. Kudos stays whole; chat stays the riffing layer. All superlatives gone (including the never-built aggregated-weights design from earlier in the session).
   - **Titles stack** (no one-per-player rule); **Worst Drawer cut**; **People's Champion rebased** to most-kudos-among-zero-win-players.
   - **New settings:** `titles_enabled` + `title_ceremony` (Streamlined: ceremony off); title badges always on final standings when titles on; **majority ready-up skip** of the ceremony.
   - **Achievement set frozen at 27 ids** (per-title first/tenth ×7, milestones, save_10/50/100, all_kudos_spent, title_collector, full_lobby, clean_sweep). Names: Renaissance Mammal, Animal Aficionado, Animal Enthusiast, Animal Hoarder (owner picks). `win_streak_3` considered, dropped.
3. **Slice 19 built** per mini-TDD (`19-title-ceremony-rework.md`): removal sweep (~29 files touched, 3 components deleted), calculator rework, settings + lobby UI, standings badges, skip-vote RPC pair. Key deviations: **ReactionGate → SocialGate rename** (it gates kudos), **`titles_awarded` payload → pid → Array**.
4. **Slice 14 built** per TDD + reconciliations: `Stats` autoload (local-first stats.json), `AchievementDefs` (27, frozen-id test pin), 3-layer idempotent Steam mirror via new Platform seam trio (ClassDB-probed: `setAchievement`/`getAchievement`/`storeStats` synchronous; `requestCurrentStats` confirmed gone). New counters: `games_full_lobby`, `clean_sweeps` (per-game transients, min 3 rounds, attended-rounds semantics).
5. **Gate/CI hardening:** round driver reworked to kudos-only social script — deterministic totals regardless of shuffle, kudos-save + wallet verified on ALL peers now. **Stats sandboxed everywhere** (GdUnit self-sandbox + per-PID paths in both CI drivers) — the pin-rule profile-pollution lesson, generalized before it bit.

6. **Owner live-tested the Slice 19 build on 3 instances** and found two issues, both fixed same session:
   - **D-key dead on expanded palette** (Slice 18 regression class): `_begin_key_draw`'s geometric canvas-rect test took the draw branch while the overlay floated over the canvas — now cross-checks `gui_get_hovered_control()`; any control hovering above the canvas wins the synthesized click. Regression-tested. **Owner confirmed both fixes live, then requested D-DRAG:** D outside the canvas is now a HELD left button (press on D-down, release-at-current-pointer on D-up) — tap = click, hold + move = full drag-and-drop (text chip into the canvas, palette favorites onto + slots). **First attempt failed live — root cause: Godot's drag detection reads the left-button mask ON MOTION EVENTS** (the GdUnit drag recipe stamps it; real motions carry none while only D is held). Fix: while the synthetic press is active, the canvas consumes each real motion and re-issues a meta-tagged copy carrying `MOUSE_BUTTON_MASK_LEFT`. Headless engagement test pins the pipeline (maskless motions → drag state + chip payload + clean D-up end); interim instant-click helper + its redundant test deleted (owner rule: no non-contributing leftovers). **OWNER-CONFIRMED live ("it works now"). 581 tests green.**
   - **Slice 20 (mini, owner-requested): undo is now RECORDED** — `UndoOp` markers ride DrawingDoc v1; final images read `effective_ops()`; replays play raw history and revert at markers (drawn → beat → poof — authentic to the player's actions, and comedy for full-replay reveals). Net mark counts for Da Vinci/Minimalist; Speed Demon/duration deliberately stay raw. Golden trick: the `undo_history` golden's baked hash IS `stroke_fill`'s — the equality pins the semantics with no re-bake. TDD: `20-authentic-undo-replay.md`.

## Verification

- Full suite: **581/581 green** (556 → 572 after the Slice 19/14 work, → 581 with Slice 20 + the D-key/D-drag tests). Two subagents updated the 8 mechanically-affected test files; calculator/ceremony/stats/undo suites written in-session.
- Owner live-confirmations this session: Slice 13-era kick previously; NEW: undo replays, D-click on expanded palette, D-drag (text chip + palette favorites) — all "works". Slice 19 ceremony/badges feel-checks and Slice 14 accrual checks remain batched (qa-backlog §19/§14/§20 residual items).
- Gates through the guarded wrapper (safe_gate.sh recreated in scratchpad): **verify_lobby, verify_round, verify_resilience — all PASS, run after the Slice 19/14 batch AND again after Slice 20**.

## Docs updated

Brief §11 amended (reactions struck, wrap-up rewritten); decision log ×2; mini-TDD + completion; Slice 14 TDD v1.1 (supersession banner + completion); impl notes ×2; qa-backlog (reaction items obsoleted, Slice 19 + 14 sections added); WHERE_WE_ARE.

## Notes for next session

- **Owner-confirmed this session (in chat): the full per-screen art pass is DONE** — WHERE_WE_ARE corrected (was "slotted after Slice 14"); only minor end polish remains.
- Slice 14's blocking toast check **cannot run on Spacewar** (fixed achievement set) — earmarked with the Slice 15 App-ID swap.
- GDScript gotcha (new): a typed ternary `x if c else ([] as Array[Dictionary])` still infers plain Array and fails assignment at runtime — use an if-statement.
- The judging-grid layout rework (fit-to-viewport columns, side-chat during judging, spotlight-on-demand) remains a polish candidate; the reaction-row removal already bought each cell ~40 px of drawing height. Analysis from this session is in the conversation record; qa-backlog Slice 19 has the density re-look item.
- QA-backlog acceleration idea (owner): scripted "guided gauntlet" run-throughs that surface many batchable checks per playthrough — needs a planning pass.
- Sound session still needs its "how do we generate assets" discussion.
