# Session Log: 2026-07-07 - Session #7

## Session Overview
**Date:** 2026-07-07 (end time not recorded precisely; owner closed at slice-completion approval)
**Duration:** One chunk, on plan (Chunk 12)
**Slice:** 9 - Connectivity & Resilience — COMPLETE (core-confirmed)
**Status:** Completed

## Objectives This Session
- Slice 9 per its full TDD: late join, disconnect/rejoin with memory, below-minimum pause + End-game-now, `fluid_rejoin` dodge guard, Slice 10 wrap-up input contract
- Slice 17/7 integration duties: rejoiners fold into the ready set un-ready; leaver-while-others-ready re-evaluation; `CustomPoolCollector.mark_departed()`

## What Was Accomplished
- **Slice 9: COMPLETE.** Built on the shipped machinery rather than the TDD's assumed mechanisms (three reconciliations caught up front by reading dependency implementation notes — zero rework): an explicit judge **rotation cursor** replaces Slice 3's modulo (identical when nobody drops; enables insert-behind-judge, ghost-skip, OFF-mode forfeit); below-minimum pause **rides the Slice 6 PAUSED pipeline** with a new `NetIds.PauseReason` (no new pause RPCs; covers deadline-less POOL_SETUP); the **wrap-up input contract folded into the results bundle** (`ended_early/rounds_played/rounds_planned/players`) so Slice 10 reads one shape for natural and early ends.
- **Resilience semantics:** quit == disconnect; retained roster entry is the memory (score in `Scoring`, kudos ledger on the entry, rotation slot never pruned); rejoin = `rebind_peer` by platform_id; submitted drawings stay judgeable after the author drops; unsubmitted dropped drawers get no card; mid-DRAWING rejoiners sit the round out host-enforced (protects their surviving submission); ghost capacity rule (connected_count, never entry count); welcome snapshot stored on Session across the Nav swap and replayed client-side through `rpc_sync_phase` (JUDGING snapshots piggyback reveal entries; POOL_SETUP snapshots carry progress).
- **Ready-up integration:** departures erase the leaver from the ready set and re-evaluate all-ready immediately — with the new ordering rule that the **below-minimum pause check runs first** (a frozen game never advances; also applies to pool-completion locking). **Judge seat holds**: no early JUDGING end without the judge's pick-gated ready — this fixed a latent Slice 17 gap (a vanished judge silently left the quorum, letting unanimous drawers force an empty-latch −1).
- **Dodge guard (fluid OFF):** flagged at disconnect (current judge, or next judge inside 30 s); armed slot reached while absent → forfeit −1 announced in the intro; window-end penalty for an absent suspect judge consumes the flag (never a double −1); rejoin before the slot clears it.
- **UI:** below-minimum pause overlay (waiting count, host-only End-game-now, chat stays live), late-join/sit-out spectator banner, coalesced join/drop/rejoin/forfeit toasts, lobby Connectivity row (Public lobby + Fluid rejoin, never preset-locked).
- **Owner-directed change (playtest day):** late joiners get the **FULL standard kudos allotment**, superseding brief §11's half rule (kudos are gifting power, not personal score) — brief amended in place, decision logged.
- **New automated gate `tools/verify_resilience.sh`** (~35 s): drop mid-DRAWING → below-minimum pause on remaining peers → rejoin → auto-resume with restored timer → kept submission wins (+2 remembered). Per-role phase-log equality, pause reason/count, status events, contract keys verified on all three peers.

## Decisions Made (see decision log)
- Slice 9 mechanism reconciliations: cursor rotation, reason-tagged pause reuse, wrap-up-in-results-bundle, penalty matrix + seat-hold, sit-out, platform_id-keyed signals, CI lessons (2026-07-07, Full entry)
- Late joiners get the full standard kudos allotment — supersedes brief §11 half rule (owner, 2026-07-07)

## Issues Encountered
- **Departure ordering bug (caught designing the CI gate, pre-gate):** all-ready re-evaluation ran before the pause check, so a 3-player leaver could advance the phase (or lock pools) instead of freezing. Fixed + regression-tested; completion satisfied during a freeze now settles on resume (`mark_returned` added for POOL_SETUP rejoiners).
- **CI driver spawn was not idempotent:** the leaver's deliberate quit reloads the main menu, which re-runs `_handle_ci_args` — a second driver spawned and fought the first (early rejoin + duplicate leave). Drivers now guard against re-spawn.
- **Cross-gate profile pollution:** gates share `user://profile.json` (`last_lobby_settings`); verify_resilience's pinned GRID/10 s settings leaked into verify_round (beat count 0). verify_round now pins reveal/replay/judging settings. **Pin rule extended: every driver pins every setting its flow depends on — including ones another gate might save.**
- Expected log noise documented: the resume broadcast races the rejoiner's navigation (RPC to not-yet-existing RoundRoot dropped; the welcome snapshot covers it).

## Tests Written/Run
- Suite grew **380 → 422 cases, all green, 0 orphans** (resilience suite 26 after the allotment-rule swap, settings/roster/rules additions 10, UI smokes 3, key-shape pins updated; one TDD-math test retired with the full-allotment decision).
- Gates via the guarded wrapper: `verify_lobby.sh` PASS, `verify_round.sh` PASS (after the pinning fix), **`verify_resilience.sh` PASS** (new).
- Owner confirmations (2026-07-07): 4-instance blocking checks — drop mid-DRAWING, below-minimum pause/resume with frozen timer, rejoin score/kudos restore, late-join placement ("seems to be working!").

## Where We Left Off
- Slices 0–9 + 16 + 17 complete. **Next session: Slice 10 — End-game Wrap-up (Chunk 13)**, fresh context.

## Notes for Next Session
- Read Slice 10's FULL TDD (`TDD/10-endgame-wrapup.md`). Its input is the **extended results bundle** (not the TDD-09 draft's standalone `get_wrapup_input()`): `ended_early/rounds_played/rounds_planned/players` (all roster entries incl. disconnected, remembered scores) + the existing `rounds/final_scores/standings/reaction_stats/kudos_stats`. Full `SessionStats` remains host-side for superlative mining. Reconcile the Slice 10 TDD to the bundle shape before coding (decision log 2026-07-07).
- Disconnected players APPEAR in wrap-up standings with remembered scores; a partial round contributes nothing; `ended_early` games route to the same sequence.
- New workflow gotchas: run gates with `safe_gate.sh` (recreate in scratchpad); CI drivers must be idempotent against menu reloads AND pin every settings key another gate might persist; `verify_resilience.sh` is the third standing gate (~35 s).
- Playtest instances were shut down at session end; none left holding the dev port.
